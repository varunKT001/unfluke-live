import React from 'react';
import { AlgorithmForm, MiniDrawer } from '../components';

const Dashboard = () => {
  return (
    <MiniDrawer>
      <AlgorithmForm />
    </MiniDrawer>
  );
};

export default Dashboard;
